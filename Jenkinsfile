pipeline{
	agent any
	stages {
  stage('Setup') {
    steps {
      // One or more steps need to be included within the steps block.
      
    }
  }

  stage('Test') {
    steps {
    sh 'mvn test -D suite=single.xml'
      // One or more steps need to be included within the steps block.
    }
  }

}
	
}